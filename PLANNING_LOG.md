2026-09-22 — Adopted planning log convention (AGENTS.md + PLANNING_LOG.md). Decided by: user.
2026-09-24 — v1 ships against a stub nutrition_rules.json (a few hand-written rules per phase/nutrient slot, no AI sourcing, no evidence grading); the Evidence Basis pipeline (Section 4 of README.md) is deferred as separate later work. Decided by: user, on Claude's recommendation.
2026-09-24 — Every stub rule must be clearly marked as a placeholder in both nutrition_rules.json and the exported groceries.md/terminal output, so it is never mistaken for real nutrition advice. Decided by: user.
2026-09-24 — Seasonality stays in v1's constraint ranking: each stub ingredient gets a minimal seasonal tag (also marked placeholder), using Northern Hemisphere seasons derived from the current month. Decided by: user.
2026-09-24 — Stub rules carry no evidence-strength label (stay honestly "placeholder" only); the constraint #2 tie-break (Strong protected over Weak) is tested via synthetic in-code fixtures, never real data. resolve_candidates() must be a pure function — takes candidates as input, never reads nutrition_rules.json itself — to be recorded as a design note when the change is written up. Decided by: user.
2026-09-24 — Rotation threshold formula resolved: N = min(max(0, 2K − P) + 1, K − 1), where P is the phase's ingredient pool size and K is the fixed number of items shown per list. Flat +1 margin (not proportional, since K is fixed and proportional-to-K reduces to a constant); capped at K − 1 so N can never reach K and make the no-repeat constraint vacuous. Verified against P = 6, 8, 20. Decided by: user, after checking Claude's worked examples.
2026-09-24 — Follow-up for tasks.md: nutrition_rules.json must be validated at load time so every phase has at least K candidate ingredients (P ≥ K), since fewer than K distinct items makes it impossible to build even one valid list. Decided by: user.
2026-09-24 — The N formula logged above (phase-wide pool P, K=6 items per list) is replaced — it doesn't match how the tool actually works: one item is picked per nutrient slot, not K items drawn together from one shared pool. Replaced by a per-nutrient-slot version: the same math applied per slot with K=1, so a slot needs at least 2 candidates to avoid a forced repeat. Decided by: user, after Claude explained the shared-pool vs. per-slot mismatch.
2026-09-24 — Each nutrient slot in the stub data must have at least 5 placeholder candidates, chosen so some can still be filtered out (e.g. by exclusions) without forcing a repeat. No-repeat rule: an ingredient must not recur for the same nutrient within the last 2–3 cycles (a rolling window, not just "different from the immediately previous cycle"). Decided by: user.
2026-09-24 — The no-repeat window is fixed at 2 cycles, not 3. Decided by: user.
2026-09-24 — The earlier tasks.md follow-up ("validate P ≥ K at load time") is retired — it was written under the old whole-list N model. The "≥5 candidates per nutrient slot" decision already covers the real requirement (3 needed for a 2-cycle window). Decided by: user.
2026-09-24 — Fallback display convention confirmed: a short per-slot note in both the terminal output and groceries.md when a repeat can't be avoided, same placement as the alternative note. No separate log file. Decided by: user.
2026-09-24 — Scope correction: this is not a grocery list and there are no recipes/meals/supplements. The tool names which nutrients matter for the current phase and, for each one, a food that contains it (e.g. "zinc is important now, eat pumpkin seeds"). Reason given: the README never says where recipes would come from, and nutrition_rules.json only holds nutrients and foods. The exported file is renamed from groceries.md to nutrients.md. README.md is being updated to match. Decided by: user.
2026-09-24 — README.md updated to match the scope correction above: title typo fixed (Calculator), groceries.md/shopping-list/meal wording replaced throughout, the "no recipes/meals/supplements" boundary added to Section 3, Section 5 point 3 and Section 6 rewritten for the per-nutrient 2-cycle rotation model (visible per-slot note, not a log), and Section 7's "No-repeat-within-N" renamed to "No-repeat-within-2-cycles" since N no longer exists. Decided by: user.
2026-09-24 — Phase boundaries: Menstrual is a fixed Day 1–5 (bleeding duration doesn't scale with cycle length); Ovulatory is a 3-day window, ovulation_day−1 to ovulation_day+1, where ovulation_day = cycle_length − 14 (ACOG/NHS: ovulation is ~14 days before the next period, not a fixed day 14); Follicular and Luteal fill the remaining days on either side. Decided by: user, after Claude's research (ACOG, NHS, Cleveland Clinic) and worked examples.
2026-09-24 — Valid --cycle-length range: 21–38 days, inclusive — lower bound from NHS (21), upper bound from FIGO (38), deliberately the wider union of the two rather than either source alone, to let as many people as possible use the tool. A 21-day cycle must NOT be rejected even though it has zero Follicular days under the phase-boundary formula above; the code must handle that empty phase without breaking. Outside 21–38, the tool rejects the input with a clear message stating the supported range. Decided by: user.
2026-09-24 — Follow-up for tasks.md: add a test covering a 21-day cycle specifically, to confirm the empty Follicular phase is handled without breaking. Decided by: user.
2026-09-24 — Input validation: `--last-period` must be strict ISO 8601 (YYYY-MM-DD); anything else is rejected with a distinct "invalid date format" error, same mechanism as the existing future-date rejection. A missing history.json is normal on first run (not an error) — the tool starts with empty history and creates the file on first write. Decided by: user.
2026-09-24 — A history.json that exists but can't be parsed is backed up to history.json.bak, then the tool proceeds with empty history (same as first run). The warning about this is terminal-only, not in nutrients.md — it's a one-time fact about this run, not a property of any specific suggestion, so it doesn't belong in a file read again later. Decided by: user, after Claude reconsidered and walked back the earlier "warn everywhere" default.
2026-09-24 — Each phase tracks exactly 3 nutrients, the same number in all four phases. Decided by: user.
2026-09-25 — A same-cycle re-run's cached pick is only reused when nothing relevant changed. --prefer-alternative persists for the rest of that cycle (a later re-run without the flag still shows the swapped item), and history.json's entry for that cycle is updated in place to record it — not treated as a new rotation event. Decided by: user.
2026-09-25 — If exclusions remove every candidate for a nutrient, the tool shows a note for that nutrient ("no suitable food left because of your exclusions") and continues with the other nutrients rather than breaking. Decided by: user.
2026-09-25 — The alternative note must state the actual constraint that decided against the alternative (e.g. "out of season" or "repeats a recent pick"), not a hardcoded phrase — the reason shown must match whichever soft constraint actually broke the tie for that candidate pair. Decided by: user.
2026-09-25 — --prefer-alternative takes the nutrient name as its argument (e.g. --prefer-alternative zinc). Decided by: user.
2026-09-25 — history.json and history.json.bak are gitignored — they store the user's period dates and the repo is public. Decided by: user.
2026-09-25 — The "no suitable food left because of your exclusions" note goes in both the terminal output and nutrients.md, same convention as the repeat and alternative notes. Decided by: user.
2026-09-25 — --cycle-length is a required argument with no default, so the phase calculation is never silently wrong for someone whose real cycle isn't 28 days. Decided by: user.
2026-09-25 — OpenSpec change "build-nutrient-planner-v1" written and validated (proposal, 4 spec deltas — cycle-phase-calculation, nutrient-selection, rotation-history, cli-output — design.md, tasks.md), capturing every decision above. Decided by: user.
2026-09-25 — --last-period validation, revised: elapsed<0 rejected (future date); 0≤elapsed<cycle_length is the normal case; cycle_length≤elapsed<cycle_length+7 is a grace period — phase forced to Luteal (not recomputed), with a "period may be late" note in both terminal and nutrients.md; elapsed≥cycle_length+7 is rejected (Mayo Clinic: >7-9 days of cycle variation is "irregular," which is out of scope). The cycle-day formula drops the modulo from tasks.md 2.4 — day is always elapsed+1, since accepted input is always within the current cycle plus grace, never wrapping to a new one. Decided by: user.
2026-09-25 — Follow-up for tasks.md: add a test verifying --prefer-alternative persistence — run once with the flag, run again in the same cycle without it, and confirm the swapped food is still shown. Decided by: user.
2026-09-25 — Every stub food entry in nutrition_rules.json must carry one or more category tags (e.g. dairy, nuts) from a fixed, known category set, so --exclude matches by category rather than by exact food name. Decided by: user.
2026-09-25 — --exclude with a value that isn't a known category (e.g. a typo like "diary") must stop the tool with an error listing the valid categories — never silently proceed as if nothing were excluded. Decided by: user.
2026-09-25 — Fixed category set for --exclude: dairy, eggs, gluten, nuts, peanuts, soy, fish, shellfish, sesame, meat, pork. Purpose: allergies and foods avoided for diet or religious reasons. A food can carry several tags; pork foods get both "meat" and "pork" (so excluding either meat generally or pork specifically works correctly). Excluding a single food by name is not part of v1 — category is the only exclusion mechanism. Decided by: user.
2026-09-25 — design.md's open question on the ≥5-candidates check is resolved: at load time, the tool warns in the terminal if any nutrient slot has fewer than 5 candidates, but keeps running rather than blocking. Decided by: user.
2026-09-25 — Category tags may be empty: a stub food carries only the tags from the fixed set of 11 that actually apply, possibly none (e.g. spinach: []). A food with no tags is never removed by --exclude. Replaces the earlier "one or more category tags" wording; task 3.4 and the nutrient-selection spec updated to match. Decided by: user.
2026-09-25 — Each phase tracks its own 3 nutrients (the count is fixed at 3, not the nutrient identities); task 3.1's "same 3 across all four phases" wording corrected to match the spec and the README demo. Decided by: user, on Claude's recommendation.
2026-09-25 — When the top two remaining candidates tie on every constraint, the first in list order is shown with no alternative note (no trade-off to surface); --prefer-alternative still swaps to the next candidate. Decided by: user, on Claude's recommendation.
2026-09-25 — Module layout (design.md open question): a single plan.py module, with nutrition_rules.json, history.json and nutrients.md written next to it. Tests use Python's built-in unittest (tests/ directory) so the project has no third-party dependencies. Decided by: Claude.
2026-09-25 — A history.json entry may carry an optional "swapped": true when its food was chosen with --prefer-alternative, so a later same-cycle re-run knows to keep the swap. Without it, a re-run could not tell a deliberate swap from a stale pick that should be recomputed. Decided by: Claude.
2026-09-25 — The change's specs, design.md and proposal.md were revised (via /opsx:update) to match what was built: cycle day is elapsed+1 with no modulo, the 7-day late-period grace window (Luteal, "period may be late" note in both terminal and nutrients.md), and the optional "swapped" history field. Decided by: user.
2026-09-25 — README Section 1's demo uses "a date 14 days ago" instead of the fixed 2026-08-25, so it shows Day 15 / Ovulatory whenever it's run. Decided by: user.
2026-09-25 — history.json keeps 3 entries per phase/nutrient (the current cycle plus the 2 before it), replacing the 2-entry cap, so a same-cycle recompute still checks both earlier cycles and the 2-cycle no-repeat rule always holds. Covered by a new test. Decided by: user, after Claude flagged the gap.
2026-09-25 — nutrients.md is gitignored alongside history.json and history.json.bak, since it reveals the user's current phase and so their period timing (public repo). Decided by: user, after Claude raised it.
